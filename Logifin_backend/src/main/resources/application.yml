spring:
  application:
    name: logifin-backend
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:LogifinLocal}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: ${SHOW_SQL:false}
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true
        # Hibernate L2 Cache - Disabled (using Spring Cache with Redis instead)
        cache:
          use_second_level_cache: false
          use_query_cache: false
    open-in-view: false

  # Redis Configuration
  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:}
    timeout: 2000
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 2
        max-wait: -1

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true

server:
  port: ${SERVER_PORT:8080}
  error:
    include-message: always
    include-binding-errors: always

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

jwt:
  secret: ${JWT_SECRET:defaultSecretKeyThatShouldBeChangedInProduction123456789}
  expiration: ${JWT_EXPIRATION:86400000}

# Email Configuration
app:
  email:
    host: ${EMAIL_HOST:smtp.gmail.com}
    port: ${EMAIL_PORT:587}
    username: ${EMAIL_USERNAME:}
    password: ${EMAIL_PASSWORD:}
    from-address: ${EMAIL_FROM_ADDRESS:noreply@logifin.com}
    from-name: ${EMAIL_FROM_NAME:Logifin}
    reset-link-base-url: ${RESET_LINK_BASE_URL:http://localhost:3000}
    token-expiration-minutes: ${TOKEN_EXPIRATION_MINUTES:30}
    enabled: ${EMAIL_ENABLED:true}
    smtp-auth: true
    smtp-start-tls-enable: true
    smtp-ssl-trust: ${EMAIL_HOST:smtp.gmail.com}

# Swagger/OpenAPI Configuration
springdoc:
  api-docs:
    enabled: ${SWAGGER_ENABLED:true}
    path: /v3/api-docs
  swagger-ui:
    enabled: ${SWAGGER_ENABLED:true}
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha
    try-it-out-enabled: true
    filter: true
    syntax-highlight:
      activated: true

# Cache Configuration
cache:
  enabled: ${CACHE_ENABLED:true}
  ttl:
    default: 60      # Default TTL in minutes
    users: 30        # User cache TTL
    roles: 120       # Role cache TTL (reference data)
    user-details: 15 # User details TTL (security sensitive)
    search: 5        # Search results TTL
    contracts: 30    # Contracts cache TTL
    loan-stages: 120 # Loan stages cache TTL (reference data)
    contract-types: 120 # Contract types cache TTL (reference data)

logging:
  level:
    root: INFO
    com.logifin: ${LOG_LEVEL:INFO}
    org.hibernate.SQL: ${SQL_LOG_LEVEL:WARN}
    org.hibernate.type.descriptor.sql.BasicBinder: ${SQL_LOG_LEVEL:WARN}
