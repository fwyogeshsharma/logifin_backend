-- V12__add_transporter_role.sql
-- ROLE_TRANSPORTER already exists in V3__add_user_roles.sql
-- This migration is kept for Flyway history consistency

-- No action needed - role already exists
SELECT 1;
